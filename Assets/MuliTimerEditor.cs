using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

//[CustomEditor(typeof(MuliTimer))]
public class NewBehaviourScript 
    //: Editor
{
    //public override void OnInspectorGUI()
    //{
    //    base.OnInspectorGUI();

    //    serializedObject.Update(); // Update serialized object

    //    MuliTimer myTarget = (MuliTimer)target;

    //    myTarget.time = EditorGUILayout.FloatField("Time Limit ", myTarget.time);
    //    myTarget.elapsedtime = EditorGUILayout.FloatField("Elapsed Time ", myTarget.elapsedtime);

    //    serializedObject.ApplyModifiedProperties(); // Apply modifications back to the serialized object
    //}
}
